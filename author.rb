class Author
  attr_accessor :first_name, :last_name, :items = []
  def initialize(first_name, last_name)
    @id = id
    @first_name = first_name
    @last_name = last_name
    @items = []
    add_author
  end

  def add_item(item)
    @items.push(item)
    item.
  end
end
